/**
 * Startup Copilot page — hosts the founder dashboard.
 */

import React from 'react';
import FounderDashboard from '../components/startup-copilot/FounderDashboard';

const FounderCopilotPage = () => <FounderDashboard />;

export default FounderCopilotPage;
